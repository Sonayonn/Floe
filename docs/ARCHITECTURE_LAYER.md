
## Venue expansion backlog (post-core, prioritized by archetype ease + testnet likelihood)
Banked from the Sui ecosystem map. Each = ~1 VenueModule file once targeted:
- TIER A (Archetype 1, SUI-native, easiest — no foreign coins): Haedal (haSUI), SpringSui,
  AlphaFi (afSUI) — stake SUI -> appreciating LST receipt; value = receipt x exchange rate.
- TIER B (Archetype 1, lending receipts): Bucket, AlphaLend, Scallop (+ Suilend/NAVI on mainnet).
- TIER C (Archetype 2, CLMM NFT — like Cetus): Turbos, FlowX, Momentum, Aftermath.
Selection criteria (learned): must have LIVE testnet deployment; Archetype 1 strongly preferred
(fungible receipt = trivial valuation, same shape as proven PLP custody). Breadth is nice-to-have;
the layer claim is already established by the uniform VenueModule interface + 2 implementations
(DeepBook live, Cetus complete). Add venues opportunistically, not on the critical path.
